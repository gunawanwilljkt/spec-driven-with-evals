# Objective: <NAME>

North star: <one paragraph — the outcome, who it's for, and why it matters. This is the intent the
whole tree answers to. Keep it stable; changing it ripples through every slice.>

Done when:
- <objective-level success criterion — observable, ideally itself eval-backed>
- <criterion 2>
- <criterion 3>

Slices (roster — a slice EXISTS iff `.sde/slices/<id>/` exists; this list is a human index):
- 01-<slug> — <one line: the thin vertical feature this slice delivers>
- 02-<slug> — <one line>

<!-- A "slice" is a thin vertical feature small enough to fit one context window comfortably.
     Prefer many small slices over few big ones: small slices are natural handoff points and keep
     each eval-trust pass cheap. -->
