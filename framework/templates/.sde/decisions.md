# Decisions — append-only ADR log

<!-- Append one block per decision. NEVER edit or delete past entries (append-only is a corruption
     guard: a torn final block is the only damage possible, and it's recoverable via git). -->

## <YYYY-MM-DDTHH:MMZ> — <short title>  [slice:<id or "objective">]
Decision: <what was decided>.
Why: <the rationale — the thing a future agent needs to not re-litigate this>.
Alternatives rejected: <option> (<one-line reason>).
By: <agent-id / owner>.
